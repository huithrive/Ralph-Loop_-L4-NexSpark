<div class="kpi-card">
  <div class="kpi-label"><%= kpi.label %></div>
  <div class="kpi-value"><%= kpi.value %></div>
  <div class="kpi-change <%= kpi.trend === 'up' ? 'up' : 'down' %>">
    <%= kpi.trend === 'up' ? '▲' : '▼' %> <%= kpi.change %>
  </div>
</div>
