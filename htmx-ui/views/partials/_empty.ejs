<div class="empty-state">
  <div class="empty-icon"><%= typeof emptyIcon !== 'undefined' ? emptyIcon : '📭' %></div>
  <div class="empty-title"><%= typeof emptyTitle !== 'undefined' ? emptyTitle : 'Nothing here yet' %></div>
  <div class="empty-desc"><%= typeof emptyDesc !== 'undefined' ? emptyDesc : 'Get started by creating something new.' %></div>
</div>
